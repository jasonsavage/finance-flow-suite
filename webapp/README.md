# Finance Flow Web Application

Web frontend for the Finance Flow application written in TypeScript + Vue 3.

## Getting Started

The web application uses the API running on port 8080. Make sure the API is running before starting the web application.

Install dependencies:

```bash
    pnpm install
```

Start the web application:

```bash
    pnpm dev
```
